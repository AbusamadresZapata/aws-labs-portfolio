version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm install --prefix invoice-digitizer/frontend
    build:
      commands:
        - npm run build --prefix invoice-digitizer/frontend
  artifacts:
    baseDirectory: invoice-digitizer/frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - invoice-digitizer/frontend/node_modules/**/*